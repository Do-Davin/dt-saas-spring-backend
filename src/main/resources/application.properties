spring.application.name=dt-saas-backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

app.jwt.secret=${APP_JWT_SECRET:dt-saas-local-dev-secret-key-minimum-32-characters-placeholder}
app.jwt.expiration-seconds=${APP_JWT_EXPIRATION_SECONDS:604800}